use editor_core::Editor;
use editor_renderer::Renderer;
use editor_window::Window;

pub struct App {
    // window: Window,
    // editor: Editor,
    // renderer: Renderer,
}

impl App {
    pub fn new() -> Self {
        Self {}
    }

    pub fn run() {}
}
